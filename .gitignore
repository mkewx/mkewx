# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Atari 2600 build artifacts (regenerated by us, not source of truth).
# Scoped to our own scratch build dir only — checkpoint .lst/.sym files
# under 2600/code/ and 2600/binaries/ are deliberately tracked evidence.
2600/build/
